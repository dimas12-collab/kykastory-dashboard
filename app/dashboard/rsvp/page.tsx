"use client"; import { useMemo,useState } from "react"; import { Download, MessageCircle, Search, RefreshCw } from "lucide-react"; import { Button,Card,Input,Badge } from "../../../components/ui"; import { rsvps } from "../../../lib/mock-data"; import { AttendanceStatus } from "../../../lib/types";
const labels:{key:AttendanceStatus|"ALL"|"MESSAGE",label:string}[]=[{key:"ALL",label:"Semua"},{key:"ATTENDING",label:"Hadir"},{key:"NOT_ATTENDING",label:"Tidak hadir"},{key:"MESSAGE",label:"Ada ucapan"}];
export default function Rsvp(){const [filter,setFilter]=useState("ALL");const [query,setQuery]=useState("");const rows=useMemo(()=>rsvps.filter(r=>(filter==="ALL"||filter==="MESSAGE"?(filter==="ALL"||!!r.message):r.attendanceStatus===filter)&&(`${r.guestName} ${r.message}`).toLowerCase().includes(query.toLowerCase())),[filter,query]);return <div><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-gold">Guest responses</p><h1 className="mt-2 font-display text-4xl">RSVP & Ucapan</h1><p className="mt-2 text-sm text-stone-500">Lihat respons dan doa dari tamu undanganmu.</p></div><div className="flex gap-2"><Button className="border border-stone-200 bg-white"><RefreshCw size={15}/> Sync data</Button><Button className="bg-ink text-white"><Download size={15}/> Export CSV</Button></div></div><div className="mt-8 grid gap-4 sm:grid-cols-4"><Metric label="Hadir" value="93"/><Metric label="Tidak hadir" value="18" red/><Metric label="Total kehadiran" value="117"/><Metric label="Ucapan" value="86"/></div><Card className="mt-6 overflow-hidden"><div className="flex flex-col gap-4 border-b border-stone-100 p-5"><div className="relative max-w-sm"><Search size={16} className="absolute left-3 top-3 text-stone-400"/><Input className="pl-9" placeholder="Cari nama atau ucapan..." value={query} onChange={e=>setQuery(e.target.value)}/></div><div className="flex gap-2 overflow-x-auto scrollbar-hide">{labels.map(x=><button key={x.key} onClick={()=>setFilter(x.key)} className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold ${filter===x.key?"bg-ink text-white":"bg-stone-100 text-stone-500"}`}>{x.label}</button>)}</div></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-stone-50 text-[11px] uppercase tracking-wider text-stone-400"><tr><th className="px-5 py-4">Nama</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Hadir</th><th className="px-5 py-4">Ucapan</th><th className="px-5 py-4">Waktu</th></tr></thead><tbody>{rows.map(r=><tr className="border-t border-stone-100" key={r.id}><td className="px-5 py-4 font-semibold">{r.guestName}</td><td className="px-5 py-4"><Badge tone={r.attendanceStatus==="ATTENDING"?"green":r.attendanceStatus==="NOT_ATTENDING"?"red":"orange"}>{r.attendanceStatus==="ATTENDING"?"Hadir":r.attendanceStatus==="NOT_ATTENDING"?"Tidak hadir":"Masih ragu"}</Badge></td><td className="px-5 py-4 text-stone-500">{r.guestCount}</td><td className="max-w-[310px] truncate px-5 py-4 text-stone-500">{r.message||"—"}</td><td className="px-5 py-4 text-xs text-stone-400">{r.submittedAt}</td></tr>)}</tbody></table>{rows.length===0&&<div className="p-12 text-center text-sm text-stone-500">Belum ada RSVP yang cocok.</div>}</div></Card></div>}
function Metric({label,value,red}:{label:string,value:string,red?:boolean}){return <Card className="p-5"><p className="text-xs text-stone-500">{label}</p><p className={`mt-2 text-3xl font-bold ${red?"text-red-500":""}`}>{value}</p></Card>}
