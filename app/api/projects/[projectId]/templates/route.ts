import { NextRequest,NextResponse } from "next/server"; import { eq } from "drizzle-orm"; import { db } from "../../../../../db"; import { messageTemplates } from "../../../../../db/schema"; import { bootstrap } from "../../../../../lib/server";
export async function GET(_:Request,{params}:{params:Promise<{projectId:string}>}){bootstrap();const {projectId}=await params;return NextResponse.json({data:db.select().from(messageTemplates).where(eq(messageTemplates.projectId,projectId)).all()});}
export async function PUT(req:NextRequest,{params}:{params:Promise<{projectId:string}>}){bootstrap();const {projectId}=await params;const body=await req.json();if(!body.content?.trim())return NextResponse.json({error:"Isi pesan wajib diisi"},{status:400});const current=db.select().from(messageTemplates).where(eq(messageTemplates.projectId,projectId)).get();if(current){db.update(messageTemplates).set({content:body.content,name:body.name||current.name,updatedAt:new Date()}).where(eq(messageTemplates.id,current.id)).run();}else db.insert(messageTemplates).values({projectId,name:body.name||"Undangan",content:body.content,isDefault:true,createdAt:new Date(),updatedAt:new Date()}).run();return NextResponse.json({ok:true});}
