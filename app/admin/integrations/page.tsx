"use client";
import { useEffect, useState } from "react";
import { CheckCircle2, RefreshCw, Send, Wifi } from "lucide-react";
import { AdminShell } from "../../../components/dashboard-shell";
import { Badge, Button, Card } from "../../../components/ui";
type Project = { id: string; coupleName: string; wordpressPostId: number | null; wordpressUrl: string | null; weddingpressSyncUrl: string | null };
export default function Integrations() { const [projects, setProjects] = useState<Project[]>([]), [selected, setSelected] = useState(""), [payload, setPayload] = useState('{"rsvps":[{"name":"Demo WeddingPress","status":"HADIR","guestCount":2,"message":"Selamat!"}]}'), [notice, setNotice] = useState(""); useEffect(() => { fetch("/api/admin/projects").then(r => r.json()).then(r => { setProjects(r.data || []); if (r.data?.[0]) setSelected(r.data[0].id); }); }, []); const project = projects.find(p => p.id === selected); const testSync = async () => { try { const response = await fetch(`/api/projects/${selected}/rsvp/sync`, { method: "POST", headers: { "Content-Type": "application/json" }, body: payload }); const result = await response.json(); setNotice(response.ok ? `${result.imported} RSVP berhasil diterima.` : result.error || "Sync gagal."); } catch { setNotice("Payload JSON tidak valid."); } }; return <AdminShell><p className="text-xs font-bold uppercase tracking-[.18em] text-gold">System</p><h1 className="mt-2 font-display text-4xl">Integrations</h1><p className="mt-2 text-sm text-stone-500">Pantau koneksi WordPress dan uji sinkronisasi WeddingPress.</p><div className="mt-8 grid gap-5 lg:grid-cols-2"><Card className="p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-emerald-50 p-2.5 text-emerald-600"><Wifi size={18}/></div><div><h2 className="font-bold">WeddingPress RSVP</h2><p className="text-xs text-stone-500">Endpoint siap menerima webhook RSVP.</p></div><Badge tone="green">Ready</Badge></div><label className="mt-6 block text-sm font-semibold">Project<select className="mt-2 h-10 w-full rounded-xl border border-stone-200 bg-white px-3 text-sm" value={selected} onChange={e => setSelected(e.target.value)}>{projects.map(p => <option key={p.id} value={p.id}>{p.coupleName}</option>)}</select></label><label className="mt-4 block text-sm font-semibold">Test payload JSON<textarea className="mt-2 min-h-32 w-full rounded-xl border border-stone-200 p-3 font-mono text-xs" value={payload} onChange={e => setPayload(e.target.value)}/></label><Button disabled={!selected} className="mt-4 bg-ink text-white" onClick={() => void testSync()}><Send size={15}/> Test sync RSVP</Button>{notice && <p className="mt-4 rounded-lg bg-emerald-50 p-3 text-xs text-emerald-700">{notice}</p>}</Card><Card className="p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-sand p-2.5 text-gold"><CheckCircle2 size={18}/></div><div><h2 className="font-bold">WordPress Bridge</h2><p className="text-xs text-stone-500">Konfigurasi per project dari halaman Invitations.</p></div></div>{project ? <div className="mt-6 space-y-4 text-sm"><Row label="Post ID" value={project.wordpressPostId?.toString() || "Belum diatur"}/><Row label="WordPress URL" value={project.wordpressUrl || "Belum diatur"}/><Row label="WeddingPress URL" value={project.weddingpressSyncUrl || "Belum diatur"}/><p className="rounded-xl bg-stone-50 p-4 text-xs leading-5 text-stone-500">Atur nilai ini di <strong>Admin → Invitations</strong>. Endpoint menerima data di <code>/api/projects/{project.id}/rsvp/sync</code>.</p></div> : <div className="mt-6 text-sm text-stone-500">Memuat project...</div>}<Button className="mt-5 border border-stone-200" onClick={() => window.location.href = "/admin/projects"}><RefreshCw size={15}/> Buka pengaturan project</Button></Card></div></AdminShell>; }
function Row({ label, value }: { label: string; value: string }) { return <div className="flex items-start justify-between gap-4 border-b border-stone-100 pb-3"><span className="text-stone-500">{label}</span><span className="max-w-[65%] break-all text-right font-semibold">{value}</span></div>; }
