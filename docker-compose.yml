services:
  kykastory:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - kykastory-data:/app/data
    restart: unless-stopped

volumes:
  kykastory-data:
